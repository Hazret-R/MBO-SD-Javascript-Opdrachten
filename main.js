// Opdracht variabelen

const userName = "Hazret";
let currentTemp = 20;

console.log(userName);
alert(`Hallo ${userName}, het is nu ${currentTemp} graden`);

// Opdracht interactie

const askedName = prompt("Wat is je naam?");
alert(askedName)
const cookiesAccepted = confirm("Accepteer je dat we je cookies mogen bewaren?")
console.log(cookiesAccepted)

const myCity = 'Den Haag';
alert('Ik woon in ' + myCity);